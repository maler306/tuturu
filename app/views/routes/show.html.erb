<p id="notice"><%= notice %></p>

<p>
  <strong>Маршрут:</strong>
  <%= @route.title %>
</p>

<p>
  <% if @trains.size==0 %>
    <strong>Поездов на маршруте нет</strong>
  <% else %>
    <strong>Поезда на маршруте:</strong>
    <%= render 'trains/trains'  %>
  <% end %>
</p>

<p>
  <% if @railway_stations.size==0 %>
    <strong>станций в маршруте нет</strong>
  <% else %>
    <strong>Список станций маршрута:</strong>
    <% @railway_stations.ordered.each do  |railway_station| %>
      <p>
        <strong>станция № </strong>
        <%= railway_station.railway_stations_routes.where(params[:route_id]).first.position %>
        <%= render railway_station  %>
      </p>
    <% end %>
  <% end %>
</p>

<%= link_to 'Редактировать', edit_route_path(@route) %> |
<%= link_to 'Назад', routes_path %>
