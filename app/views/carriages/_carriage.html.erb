      <tr>
        <td><%= @carriage.number %></td>
        <td><%= Carriage.types[@carriage.carriage_type.to_sym] %></td>
        <td><%= @carriage.bottom_seats %></td>
        <td><%= @carriage.top_seats %></td>
        <td><%= link_to 'Показать', train_carriage_path(@train, @carriage) %></td>
        <td><%= link_to 'Редактировать', edit_train_carriage_path(@train, @carriage) %></td>
        <td><%= link_to 'Удалить', train_carriage_path(@train, @carriage), method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
