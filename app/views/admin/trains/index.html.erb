<h1><%= t('.title') %></h1>

<table>
  <thead>
    <tr>
      <th><%=  t('admin.trains.attributes.train_number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('.new_train') , new_admin_train_path %>
